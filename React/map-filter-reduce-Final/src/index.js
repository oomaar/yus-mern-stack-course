import emojipedia from "./emojipedia.js";

//Chalenge of video 24
// console.log(emojipedia);
// const newData = emojipedia.map((emojiEntry) => console.log(emojiEntry.meaning.substring(0, 100)));

var numbers = [3, 56, 2, 48, 5];

// Arrow Functions + Review on map, fiter, reduce, find and findindex
//Map -Create a new array by doing something with each item in an array.
// const newNumbers = numbers.map(num => num * 2);
// console.log(newNumbers);

//Filter - Create a new array by keeping the items that return true.
// const newNumbers = numbers.filter(num => num < 10);
// console.log(newNumbers);

//Reduce - Accumulate a value by doing something to each item in an array.
// const newNumbers = numbers.reduce((accumulator, num) => (accumulator + num));
// console.log(newNumbers);

//Find - find the first item that matches from an array.
// const newNumbers = numbers.find(num => num > 10);
// console.log(newNumbers);

//FindIndex - find the index of the first item that matches.
// const newNumbers = numbers.findIndex(num => num > 10);
// console.log(newNumbers);